language: node_js
node_js:
  - "8.0"
script: 
  - echo "Deploying..."
install:
  - npm install -g firebase-tools
after_success:
  - echo "$FIREBASE_TOKEN"
  - firebase deploy --token "$FIREBASE_TOKEN" -P book-shelf-be347